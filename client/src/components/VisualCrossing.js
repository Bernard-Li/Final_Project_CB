const VisualCrossing = (props) => {
  const VisualCrossingKey = process.env.VISUAL_CROSSING_KEY;
  //Return current weather on homepage
  

  //Return weather for the destination based on what the user entered - autocomplete search?
  return (
    <>

    </>
  )
}

export default VisualCrossing;